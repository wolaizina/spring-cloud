server:
  port: 8085
eureka:
  client:
    service-url:
      #?????????
      defaultZone: http://localhost:8080/eureka
spring:
  application:
    #?????????????????? ?????????????????
    name: cloud-feign
feign:
  hystrix:
    enabled: true